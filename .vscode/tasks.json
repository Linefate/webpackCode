{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean-dist",
            "type": "shell",
            "command": "rm",
            "args": [
                "-rf",
                "${workspaceFolder}/debug/dist"
            ],
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
